# Анализ Графа Навыков и Создание Студентов

Набор скриптов для парсинга, анализа и визуализации графа навыков из базы данных Django, а также для создания студентов с симуляцией обучения.

## Файлы

### Анализ графа навыков

### `parse_skills_graph.py`
Основной скрипт для анализа графа навыков. Содержит класс `SkillsGraphParser` с полным функционалом.

**Возможности:**
- Парсинг графа навыков из базы данных
- Анализ структуры графа (глубина, циклы, статистика)
- Анализ прогресса студентов по навыкам  
- Построение путей изучения для навыков
- Экспорт в различные форматы (JSON, DOT, визуализация)
- Детальные отчеты

### `test_skills_parser.py`
Быстрые тесты для проверки функциональности парсера.

### `analyze_skills_graph.py`
Django management команда для удобного запуска анализа.

### Построение траектории обучения

### `learning_trajectory_builder.py`
Скрипт для построения траектории обучения студента на основе графа навыков.

**Возможности:**
- Симуляция логичного освоения навыков с учетом prerequisites
- Валидация согласованности освоения навыков
- Генерация рекомендаций по изучению
- Анализ прогресса по глубинам навыков
- Экспорт траектории обучения

### `test_trajectory_builder.py`
Тесты для проверки построения траектории обучения.

### `build_learning_trajectory.py`
Django management команда для построения траектории.

### Создание студентов с попытками

### `create_student_with_attempts.py`
Скрипт для создания студента и симуляции попыток решения заданий.

**Возможности:**
- Создание полноценного профиля студента
- Симуляция попыток решения заданий на основе целевой траектории
- Автоматическое применение BKT для обновления навыков
- Генерация реалистичных временных меток и результатов
- Анализ получившихся уровней освоения

### `test_student_creator.py`
Тесты для проверки создания студента.

### `create_student_with_attempts.py` (management command)
Django management команда для создания студента.

## Использование

### 1. Анализ графа навыков
```bash
# Полный анализ
python manage.py analyze_skills_graph

# Только экспорт данных
python manage.py analyze_skills_graph --export-only

# Путь изучения для конкретного навыка
python manage.py analyze_skills_graph --target-skill 123
```

### 2. Построение траектории обучения
```bash
# Стандартная траектория (12 полностью + 8 частично освоенных навыков)
python manage.py build_learning_trajectory

# Кастомная траектория
python manage.py build_learning_trajectory --mastered 15 --partial 10

# С воспроизводимостью
python manage.py build_learning_trajectory --seed 42
```

### 3. Создание студента с попытками
```bash
# Создание студента Клементьева А.А.
python manage.py create_student_with_attempts

# С кастомными параметрами обучения
python manage.py create_student_with_attempts --mastered 15 --partial 10

# Пересоздание существующего студента
python manage.py create_student_with_attempts --recreate

# С воспроизводимостью
python manage.py create_student_with_attempts --seed 42
```

### 4. Через Django shell
```python
# Анализ графа навыков
python manage.py shell
exec(open('mlmodels/tests/parse_skills_graph.py').read())

# Построение траектории
exec(open('mlmodels/tests/learning_trajectory_builder.py').read())

# Создание студента
exec(open('mlmodels/tests/create_student_with_attempts.py').read())
```

### 5. Быстрые тесты
```python
python manage.py shell
exec(open('mlmodels/tests/test_skills_parser.py').read())
exec(open('mlmodels/tests/test_trajectory_builder.py').read())
exec(open('mlmodels/tests/test_student_creator.py').read())
```

## Выходные данные

### Экспорт файлы
- `skills_graph.json` - граф навыков в JSON формате
- `skills_graph_viz.json` - данные для визуализации
- `skills_graph.dot` - граф в DOT формате для Graphviz

### Отчет включает
- Общая статистика графа (количество навыков, связей)
- Структурный анализ (корневые навыки, листовые, глубина)
- Обнаружение циклов в зависимостях
- Статистика прогресса студентов
- Топ навыков по различным метрикам

## Структура данных

### Граф навыков
```json
{
  "skill_id": [prerequisite_id1, prerequisite_id2, ...]
}
```

### Обратный граф  
```json
{
  "prerequisite_id": [dependent_skill_id1, dependent_skill_id2, ...]
}
```

### Связи заданий и навыков
```json
{
  "task_id": [skill_id1, skill_id2, ...],
  "skill_id": [task_id1, task_id2, ...]
}
```

## Визуализация

Сгенерированный DOT файл можно визуализировать с помощью Graphviz:

```bash
dot -Tpng skills_graph.dot -o skills_graph.png
```

## Анализ циклов

Скрипт автоматически обнаруживает циклы в зависимостях навыков, что может указывать на проблемы в структуре курса.

## Пути изучения

Для любого навыка можно построить оптимальный путь изучения, учитывающий все prerequisite зависимости.

## Примеры использования

### Найти все базовые навыки
```python
parser = SkillsGraphParser()
parser.parse_skills_graph()
root_skills = [skill_id for skill_id, prereqs in parser.skills_graph.items() if not prereqs]
```

### Найти навыки определенной глубины
```python
analysis = parser.analyze_graph_structure()
depth_2_skills = [skill_id for skill_id, depth in analysis['skill_depths'].items() if depth == 2]
```

### Получить статистику по курсу
```python
course_skills = Skill.objects.filter(courses__id=course_id).values_list('id', flat=True)
course_graph = {k: v for k, v in parser.skills_graph.items() if k in course_skills}
```

## Создание студента Клементьева А.А.

Скрипт `create_student_with_attempts.py` создает студента с полным профилем:

**Данные студента:**
- ФИО: Клементьев Алексей Александрович
- Email: Alex0.oKlem@gmail.com
- Организация: РГПУ им. Герцена
- Username: alex_klementev

**Процесс симуляции обучения:**

1. **Анализ графа навыков** - определение структуры и зависимостей
2. **Генерация целевой траектории** - случайное но логичное распределение уровней освоения
3. **Создание попыток решения заданий** - для каждого навыка генерируются попытки:
   - Количество попыток зависит от целевого уровня освоения
   - Успешность улучшается с опытом (имитация обучения)
   - Время решения зависит от сложности задания
   - Соблюдается хронологический порядок по глубине навыков

4. **Применение BKT** - каждая попытка автоматически обновляет вероятности освоения навыков через алгоритм Bayesian Knowledge Tracing при сохранении в базу данных

5. **Валидация результата** - проверка логической согласованности освоения

**Результат:**
- Студент с реалистичной историей обучения
- Корректные уровни освоения навыков (соответствующие графу зависимостей)
- Попытки решения заданий с метаданными
- Обновленные BKT параметры для всех затронутых навыков

**Пример результата:**
```
📊 СТАТИСТИКА ОБУЧЕНИЯ:
   • Всего попыток: 127
   • Правильных ответов: 89
   • Общая успешность: 70.1%
   • Навыков с данными: 20

✅ ПОЛНОСТЬЮ ОСВОЕННЫЕ НАВЫКИ (12):
   • Введение в ООП: 0.96 (8/10 попыток)
   • Основы алгоритмов: 0.90 (7/9 попыток)
   • Переменные и типы данных: 0.99 (9/10 попыток)

🟡 ЧАСТИЧНО ОСВОЕННЫЕ НАВЫКИ (8):
   • Сложность алгоритмов: 0.41 (3/7 попыток)
   • Условные операторы: 0.66 (4/6 попыток)
```
